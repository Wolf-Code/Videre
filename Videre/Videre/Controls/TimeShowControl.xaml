<UserControl x:Class="Videre.Controls.TimeShowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignWidth="300" Width="200" IsHitTestVisible="False">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Viewbox Grid.Column="0" Grid.Row="0">
            <Label Name="TimeLabel" Foreground="Black" Background="White"/>
        </Viewbox>

        <Canvas Grid.Row="1">
            <Polygon HorizontalAlignment="Center" Name="Pointer" Points="-30,0 0,20, 30,0" Stroke="White" Fill="White"/>
        </Canvas>
    </Grid>
</UserControl>
