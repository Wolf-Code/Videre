<local:VidereControl x:Class="Videre.Controls.LibraryDirectorySelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Videre.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <GroupBox Header="Directories" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
            <ListBox Name="DirectoryList" SelectionMode="Multiple" SelectionChanged="OnSelectionChange"/>
        </GroupBox>
        <Button Grid.Column="0" Grid.Row="1" Margin="5" Click="OnAddNewDirectory">Add</Button>
        <Button Name="RemoveButton" Grid.Column="1" Grid.Row="1" Margin="5" Click="OnRemoveExistingDirectory" IsEnabled="False">Remove</Button>
    </Grid>
</local:VidereControl>
